
export const wall = {
  vert: require('./wall.vert.glsl'),
  frag: require('./wall.frag.glsl')
};

export const particle = {
  vert: require('./particle.vert.glsl'),
  frag: require('./particle.frag.glsl')
};

export const postFX = {
  vert: require('./postFX.vert.glsl'),
  frag: require('./postFX.frag.glsl')
};